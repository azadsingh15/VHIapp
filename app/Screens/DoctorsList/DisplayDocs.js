import React from "react";
import VerticalListDocs from "./VerticalListDocs";

const DisplayDocs = ({ data, navigation }) => {
  return <VerticalListDocs data={data} navigation={navigation} />;
};
export default DisplayDocs;
